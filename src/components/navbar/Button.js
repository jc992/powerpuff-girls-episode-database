import React from 'react';

/**
 * Renders a home button in navbar.
 */
export const Button = () => {
    return (
        <button className="btn btn--black">Home</button>
    );
};

export default Button;